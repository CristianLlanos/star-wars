# Client-side calls (browser) should hit localhost
NEXT_PUBLIC_API_BASE_URL=http://localhost:8800

# Server-side calls (inside the web container) can use the docker network name
API_BASE_URL=http://api:8000
